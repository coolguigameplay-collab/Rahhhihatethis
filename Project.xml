<?xml version="1.0" encoding="utf-8"?>
<project>
	<!-- _________________________ Application Settings _________________________ -->

	<app title="BFEXEOPT"
		file="BFEXEOPT"
		packageName="com.bfexeopt.engine"
		package="com.bfexeopt.engine"
		main="Main"
		version="0.2.8"
		company="BFEXEOPT" />

	<set name="APP_ID" value="0x0100f6c013bbc000" />

	<haxedef name="LINC_LUA_RELATIVE_DYNAMIC_LIB"/>

	<app preloader="flixel.system.FlxPreloader" />

	<set name="SWF_VERSION" value="11.8" />

	<!-- ______________________ BFEXEOPT CUSTOMIZATION ______________________ -->

	<define name="MODS_ALLOWED" if="desktop || mobile" />
	<define name="HSCRIPT_ALLOWED" if="desktop || mobile" />
	<define name="LUA_ALLOWED" if="cpp" />
	<define name="ACHIEVEMENTS_ALLOWED" />
	<define name="DISCORD_ALLOWED" if="desktop" />
	<define name="TRANSLATIONS_ALLOWED" />
	<define name="IRIS_DEBUG"/>

	<define name="VIDEOS_ALLOWED"
		if="desktop || mobile"
		unless="32bits"/>

	<define name="MULTITHREADED_LOADING" if="cpp" />
	<define name="SHOW_LOADING_SCREEN" />
	<define name="PSYCH_WATERMARKS"/>

	<section if="officialBuild">
		<define name="TITLE_SCREEN_EASTER_EGG"/>
		<define name="BASE_GAME_FILES" />
	</section>

	<!-- ____________________________ Window Settings ___________________________ -->

	<window
		width="1280"
		height="720"
		fps="60"
		background="#000000"
		hardware="true"
		vsync="false"
		allow-high-dpi="true" />

	<window
		if="html5"
		resizable="true" />

	<window
		if="desktop"
		orientation="landscape"
		fullscreen="false"
		resizable="true"
		vsync="false"/>

	<window
		if="mobile"
		orientation="landscape"
		fullscreen="true"
		resizable="false"
		allow-shaders="true"
		require-shaders="true" />

	<window
		if="switch"
		orientation="landscape"
		fullscreen="true"
		width="0"
		height="0"
		resizable="true" />

	<!-- _____________________________ Path Settings ____________________________ -->

	<set name="BUILD_DIR"
		value="export/debug"
		if="debug" />

	<set name="BUILD_DIR"
		value="export/release"
		unless="debug" />

	<set name="BUILD_DIR"
		value="export/32bit"
		if="32bits" />

	<classpath name="source" />

	<!-- __________________________ Game Assets & Etc. __________________________ -->

	<assets path="assets/fonts" />

	<assets path="assets/shared"
		exclude="*.ogg"
		if="web"/>

	<assets path="assets/shared"
		exclude="*.mp3"
		unless="web"/>

	<assets path="assets/embed"
		exclude="*.ogg"
		if="web"
		embed="true"/>

	<assets path="assets/embed"
		exclude="*.mp3"
		unless="web"
		embed="true"/>

	<assets path="assets/videos"
		if="VIDEOS_ALLOWED"/>

	<assets path="assets/songs"
		exclude="*.ogg"
		if="web"/>

	<assets path="assets/songs"
		exclude="*.mp3"
		unless="web"/>

	<assets path="assets/week_assets"
		rename="assets"
		exclude="*.ogg"
		if="web"/>

	<assets path="assets/week_assets"
		rename="assets"
		exclude="*.mp3"
		unless="web"/>

	<!-- __________________________ Title Screen Easter Egg __________________________ -->

	<section if="TITLE_SCREEN_EASTER_EGG">
		<assets path="assets/secrets"
			rename="assets/shared"
			exclude="*.ogg"
			if="web"/>

		<assets path="assets/secrets"
			rename="assets/shared"
			exclude="*.mp3"
			unless="web"/>
	</section>

	<!-- __________________________ Translations __________________________ -->

	<section if="TRANSLATIONS_ALLOWED">
		<assets path="assets/translations"
			rename="assets"
			exclude="*.ogg"
			if="web"/>

		<assets path="assets/translations"
			rename="assets"
			exclude="*.mp3"
			unless="web"/>
	</section>

	<!-- __________________________ Base Game Files __________________________ -->

	<section if="BASE_GAME_FILES">
		<assets path="assets/base_game"
			rename="assets"
			exclude="*.ogg"
			if="web"/>

		<assets path="assets/base_game"
			rename="assets"
			exclude="*.mp3"
			unless="web"/>

		<haxelib name="grig.audio"
			if="BASE_GAME_FILES"/>

		<haxelib name="funkin.vis"
			if="BASE_GAME_FILES"/>
	</section>

	<define name="CHECK_FOR_UPDATES"
		if="officialBuild"/>

	<!-- ______________________________ BFEXEOPT MOD PACKAGE ______________________________ -->

	<section if="MODS_ALLOWED">

		<!--
			Packaged mod container.

			Source:
			BFEXEOPT/
			└── mods/
				└── TestMod/
					└── test.txt

			APK:
			assets/BFEXEOPT/mods/TestMod/test.txt

			Extracted:
			/storage/emulated/0/.BFEXEOPT/mods/TestMod/test.txt
		-->

		<assets path="BFEXEOPT"
            rename="BFEXEOPT"
                  include="*" />

		<assets path="BFEXEOPTList.txt"
			rename="BFEXEOPTList.txt" />

	</section>

	<!-- ______________________________ Readme ______________________________ -->

	<assets path="art/readme.txt"
		rename="do NOT readme.txt"
		unless="mobile" />

	<!-- ______________________________ OpenAL config ______________________________ -->

	<section if="desktop">

		<assets path="alsoft.txt"
			rename="plugins/alsoft.ini"
			type="text"
			if="windows"/>

		<assets path="alsoft.txt"
			rename="plugins/alsoft.conf"
			type="text"
			unless="windows"/>

	</section>

	<!-- _______________________________ Libraries ______________________________ -->

	<haxelib name="flixel"/>
	<haxelib name="flixel-addons"
		version="3.2.2"/>
	<haxelib name="tjson"
		version="1.4.0"/>

	<!-- ______________________________ Psych Engine Libraries ______________________________ -->

	<haxelib name="linc_luajit"
		if="LUA_ALLOWED"/>

	<haxelib name="hscript-iris"
		if="HSCRIPT_ALLOWED"/>

	<haxelib name="hxvlc"
		if="VIDEOS_ALLOWED"/>

	<haxelib name="hxdiscord_rpc"
		if="DISCORD_ALLOWED"/>

	<haxelib name="flxanimate"/>

	<!-- ______________________________ Discord ______________________________ -->

	<haxedef name="DISCORD_DISABLE_IO_THREAD"
		if="hxdiscord_rpc" />

	<haxedef name="NO_PRECOMPILED_HEADERS"
		if="linux" />

	<!-- ______________________________ Crash Handler ______________________________ -->

	<haxelib name="hxcpp-debug-server"
		if="debug"/>

	<haxedef name="HXC_LIBVLC_LOGGING"
		if="VIDEOS_ALLOWED debug" />

	<haxedef name="HXVLC_NO_SHARE_DIRECTORY"
		if="VIDEOS_ALLOWED" />

	<haxedef name="HXVLC_EXPERIMENTAL_WINDOWSARM"
		if="windows HXCPP_ARM64" />

	<define name="x86_BUILD"
		if="32bits" />

	<!-- ______________________________ Haxedefines ______________________________ -->

	<haxedef name="FLX_NO_FOCUS_LOST_SCREEN" />

	<haxedef name="HXC_DEBUG_TRACE"
		if="debug" />

	<haxedef name="FLX_NO_DEBUG"
		unless="debug" />

	<haxedef name="NAPE_RELEASE_BUILD"
		unless="debug" />

	<!-- ______________________________ Crash Handler HaxeCPP ______________________________ -->

	<section if="cpp">

		<haxedef name="HXCPP_CHECK_POINTER" />
		<haxedef name="HXCPP_STACK_LINE" />
		<haxedef name="HXCPP_STACK_TRACE" />
		<haxedef name="HXCPP_CATCH_SEGV" />

	</section>

	<!-- ______________________________ Windows ______________________________ -->

	<app path="export\32bit\windows\bin"
		if="32bits" />

	<!-- ______________________________ Warnings ______________________________ -->

	<haxedef name="no-deprecation-warnings" />

	<haxedef name="message.reporting"
		value="pretty" />

	<!-- ______________________________ Android Certificate ______________________________ -->

	<certificate
		path="key.keystore"
		password="psychengine"
		alias="psychport"
		alias-password="psychengine"
		if="android"
		unless="debug" />

	<!-- ______________________________ Apple Systems ______________________________ -->

	<config:ios
		category_type="public.app-category.music-games" />

	<config:mac
		category_type="public.app-category.music-games" />

	<!-- ______________________________ Mobile Files ______________________________ -->

	<haxedef name="SHARE_MOBILE_FILES"
		if="mobile" />

	<!-- ______________________________ Macro Fixes ______________________________ -->

	<haxeflag name="--macro"
		value="allowPackage('flash')" />

	<haxeflag name="--macro"
		value="include('my.pack')" />

	<haxedef name="hscriptPos"
		if="HSCRIPT_ALLOWED" />

	<!-- _________________________________ Custom _______________________________ -->

	<assets path="art/icons/iconOG.png"
		rename="icon.png"
		if="linux || mac" />

	<icon path="art/icons/icon16.png"
		size="16"/>

	<icon path="art/icons/icon32.png"
		size="32"/>

	<icon path="art/icons/icon64.png"
		size="64"/>

	<icon path="art/icons/iconOG.png" />

</project>
