name: Build

on:
  workflow_call:
    inputs:
      name:
        required: true
        type: string
      os:
        required: true
        type: string
      buildArgs:
        required: true
        type: string
      artifactName:
        required: true
        type: string
      artifactPath:
        required: true
        type: string

env:
  HAXE_VERSION: 4.3.4
  PROJECT_NAME: BFEXEOPT
  ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION: true

jobs:
  build:
    name: Build
    runs-on: ${{ inputs.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@main

      - name: Setup Haxe
        uses: krdlab/setup-haxe@master
        with:
          haxe-version: ${{ env.HAXE_VERSION }}

      - name: Install Libraries
        run: |
          haxelib install hmm --quiet
          haxelib run hmm install --quiet

      - name: Patch Flixel Android ARM64 manifest bug
        if: inputs.name == 'Android ARM64'
        run: |
          HAXELIB_REPO=$(haxelib config)
          FLIXEL_INCLUDE="$HAXELIB_REPO/flixel/5,6,1/include.xml"
          echo "Patching $FLIXEL_INCLUDE"
          test -f "$FLIXEL_INCLUDE" || { echo "include.xml not found in $FLIXEL_INCLUDE"; exit 1; }
          perl -pi -e 's/ if="\$\{lime &ge; 8\.2\.0\}"//' "$FLIXEL_INCLUDE"
          grep -n "appCategory" "$FLIXEL_INCLUDE"

      - name: Patch Flixel Android ARMv7 manifest bug
        if: inputs.name == 'Android ARMv7'
        run: |
          HAXELIB_REPO=$(haxelib config)
          FLIXEL_INCLUDE="$HAXELIB_REPO/flixel/5,6,1/include.xml"
          echo "Patching $FLIXEL_INCLUDE"
          test -f "$FLIXEL_INCLUDE" || { echo "include.xml not found in $FLIXEL_INCLUDE"; exit 1; }
          perl -pi -e 's/ if="\$\{lime &ge; 8\.2\.0\}"//' "$FLIXEL_INCLUDE"
          grep -n "appCategory" "$FLIXEL_INCLUDE"

      - name: Configure Android ARM64
        if: inputs.name == 'Android ARM64'
        run: |
          haxelib run lime config ANDROID_SDK $ANDROID_HOME
          haxelib run lime config ANDROID_NDK_ROOT $ANDROID_NDK_LATEST_HOME
          haxelib run lime config JAVA_HOME $JAVA_HOME_17_arm64
          haxelib run lime config ANDROID_SETUP true

      - name: Configure Android ARMv7
        if: inputs.name == 'Android ARMv7'
        run: |
          haxelib run lime config ANDROID_SDK $ANDROID_HOME
          haxelib run lime config ANDROID_NDK_ROOT $ANDROID_NDK_LATEST_HOME
          haxelib run lime config JAVA_HOME $JAVA_HOME_17_arm64
          haxelib run lime config ANDROID_SETUP true

      - name: Compile
        run: haxelib run lime build ${{ inputs.buildArgs }}

      - name: Upload Artifact
        uses: actions/upload-artifact@main
        with:
          name: ${{ inputs.artifactName }}
          path: ${{ inputs.artifactPath }}
          if-no-files-found: error
